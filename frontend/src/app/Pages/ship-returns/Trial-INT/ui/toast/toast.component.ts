import { Component } from '@angular/core';

/** Legacy import shim. Rendering is handled by the global ngx-sonner toaster. */
@Component({
  selector: 'app-toast',
  standalone: true,
  template: '',
})
export class ToastComponent {}
